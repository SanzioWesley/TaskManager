version: '3.8'

services:
  # Banco de Dados SQL Server
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: taskmanager-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=TaskManager@123
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - taskmanager-network

  # Backend .NET (vamos configurar depois, por enquanto só comentado)
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: taskmanager-backend
  #   ports:
  #     - "5000:80"
  #   depends_on:
  #     - sqlserver
  #   environment:
  #     - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=TaskManagerDB;User=sa;Password=TaskManager@123;TrustServerCertificate=true
  #   networks:
  #     - taskmanager-network

  # Frontend React (vamos configurar depois)
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: taskmanager-frontend
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - backend
  #   networks:
  #     - taskmanager-network

volumes:
  sqlserver-data:

networks:
  taskmanager-network:
    driver: bridge